<h1 id="album-for">Albums for : {{album.[0].artists.[0].name}}</h1>
<div class="container">
{{#each album}}
<div class="result">
  <div>
<img src="{{images.[0].url}}" alt="">
<h4>{{name}}</h4>
<a href="/tracks/{{id}}">View Tracks</a>
</div>
</div>
{{/each}}

</div>
